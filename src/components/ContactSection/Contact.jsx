import { Form } from "./Form"
import { Map } from "./Map"


export const Contact = () => {
    return (
        <section className="contact-container">
            <Map />
            
            <Form />
        </section>
    )
}
